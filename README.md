# Арена Переговоров

Тренажёр переговорных навыков с ИИ-собеседником. Построен на Rust (Axum), использует Groq API для генерации диалогов и TTS/STT для голосового режима.

## Возможности

- **6 сценариев** — продажи, закупки, HR, управление, партнёрство, продление контракта
- **ИИ-диалог** — собеседник отвечает через Groq API (qwen3.8-27b), адаптируется к вашей стратегии
- **Метод SPIN** — отслеживание Situation / Problem / Implication / Need-payoff вопросов
- **Гарвардский метод** — фокус на интересах vs позициях, объективные критерии
- **Голосовой режим** — TTS/STT через Silero (опционально)
- **Аналитика** — оценка стратегии, аргументов и тона; инсайты и рекомендации
- **AI-генератор сценариев** — создание пользовательских сценариев через Groq
- **Админ-панель** — управление сценариями, пользователями, ролями и настройками
- **Dashboard** — выбор сценариев, история сессий, генерация новых сценариев

## Технологии

| Компонент | Технология |
|-----------|-----------|
| Backend | Rust + Axum |
| Шаблоны | Tera |
| БД | SQLite (rusqlite) |
| Auth | Argon2 + JWT |
| ИИ | Groq Cloud API |
| TTS/STT | Silero TTS (опционально) |

## Получение API-ключа Groq

Проект использует **Groq Cloud API** для генерации ответов ИИ-собеседника. Модели хостятся на стороне Groq — скачивать ничего не нужно.

### Используемая модель

| Модель | Назначение | Скорость |
|--------|-----------|----------|
| `qwen/qwen3.8-27b` | Диалоги + генерация сценариев | ~150 t/s |

### Пошаговая инструкция

1. **Регистрация** на [console.groq.com](https://console.groq.com)
   - Поддерживается вход через Google / GitHub / Email

2. **Создание API-ключа**
   - В консоли нажмите **API Keys** в левом меню
   - Нажмите **Create API Key**
   - Задайте имя (например `negotiation-arena`)
   - Скопируйте ключ — он показывается только один раз

3. **Лимиты бесплатного плана**
   - 30 RPM (запросов в минуту)
   - 14 400 токенов в минуту
   - Для тренажёна более чем достаточно

4. **Добавьте ключ в `.env`**
   ```bash
   cp .env.example .env
   ```
   Откройте `.env` и вставьте ключ:
   ```
   GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxx
   ```

> **Альтернатива:** если хотите использовать другого провайдера (OpenAI, Anthropic, Ollama локально) — потребуется изменить код в `engine.rs` и `main.rs` (替换 API calls).

## Быстрый старт

### 1. Требования

- **Rust** 1.75+ ([rustup.rs](https://rustup.rs))
- **API-ключ Groq** (см. раздел выше)

### 2. Клонирование и запуск

```bash
git clone https://github.com/ApollieKastro/negotiation-arena.git
cd negotiation-arena

# Настройка окружения
cp .env.example .env
# Вставьте ваш GROQ_API_KEY в .env

# Запуск
cargo run
```

Приложение будет доступно на `http://localhost:3001`.

### 3. Вход

- **Логин:** `admin`
- **Пароль:** `admin123` (или значение `ADMIN_PASSWORD` из .env)

### 4. Опционально: TTS/STT

Для голосового режима нужен работающий Silero TTS на порту 8080:

```bash
# Клонируйте и запустите Silero TTS отдельно
git clone https://github.com/snakers4/silero-tts.git
cd silero-tts
pip install -r requirements.txt
python server.py  # запустится на :8080
```

## Структура проекта

```
negotiation-arena/
├── src/
│   ├── main.rs          # Сервер, маршруты, middleware
│   ├── engine.rs         # Сценарии, ИИ-генерация ответов, SPIN-анализ
│   ├── models.rs         # Данные: сценарии, диалоги, сессии
│   ├── db.rs             # SQLite, пользователи, JWT, история
│   └── tts.rs            # TTS/STT (Silero)
├── templates/            # Tera-шаблоны (HTML)
├── static/               # CSS, JS
├── .env.example          # Шаблон переменных окружения
└── Cargo.toml
```

## Переменные окружения

| Переменная | Описание | По умолчанию |
|-----------|----------|-------------|
| `GROQ_API_KEY` | API-ключ Groq | — (обязательно) |
| `JWT_SECRET` | Секрет для JWT-токенов | `change_me_to_a_strong_random_secret` |
| `ADMIN_PASSWORD` | Пароль администратора | `admin123` |
| `PORT` | Порт сервера | `3001` |

## Лицензия

MIT
